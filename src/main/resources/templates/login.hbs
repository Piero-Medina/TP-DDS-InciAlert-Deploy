<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Login</title>
</head>
<body>

    <style>
    .form-signin {
        width: 100%;
        max-width: 300px; /* ajusta el ancho máximo según tus necesidades */
        margin: auto;
        margin-top: 100px;

    }
    body {
        background-color: #0a2642;
        color: white;
        margin: 0; /* Agrega esto para eliminar los márgenes predeterminados del cuerpo */
    }
    .form-control {
            background-color: #35424a; /* Cambia este color de fondo */
            color: #35424a; /* Cambia este color de letras */
            border: 1px solid #35424a; /* Cambia este color según tus preferencias */
        }
    </style>

    <main class="form-signin text-center">
    <form method="post" action="/login">
        <img class="mb-4" src="img/people.svg" alt="" width="72" height="57">
        <h1 class="h3 mb-3 fw-normal">Inicio de Sesión</h1>

        <div class="form-floating">
            <input type="text" class="form-control" id="floatingInput" name ="usuario" placeholder="Usuario">
            <label for="floatingInput">Usuario</label>
            
        </div>
        
        <div class="form-floating mt-3">
            <input type="password" class="form-control" id="floatingPassword" name = "contrasenia" placeholder="Contraseña">
            <label for="floatingPassword">Contraseña</label>
        </div>

        <div class="form-check text-start mt-3">
            <input class="form-check-input" type="checkbox" value="remember-me" id="flexCheckDefault">
            <label class="form-check-label" for="flexCheckDefault">
                Recordarme
            </label>
        </div>
        
        <button class="btn btn-primary mt-3 w-100" type="submit">Iniciar Sesión</button>
        
        <a href="#" class="btn btn-secondary mt-3 w-100" onclick="volverAtras()">Volver</a>
    </form>
    <br>

        {{#if errorUsuario}}
            <p class="text-danger">¡Error! Usuario incorrecto.</p>
        {{/if}}

        {{#if errorContrasenia}}
            <p class="text-danger">!Error! Contraseña incorrecta.</p>
        {{/if}}
    </main>
    <script>
        function volverAtras() {
            window.history.back();
        }
    </script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
